import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'brasil_a_mesa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
